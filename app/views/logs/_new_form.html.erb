<%= form_for(@log) do |f| %>
  <%= hidden_field_tag :divelog_id, @divelog.id %>
  <div class="row">
    <div class="col-md-8 col-md-offset-1">
      <div class="field">
        <%= f.text_area :content, placeholder: "メモ" %>
      </div>
    </div>
    <div class="col-md-3">
      <%= f.submit "メモ追加", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>

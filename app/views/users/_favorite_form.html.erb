<div id="favorite-<%= @divelog.id %>">
  <% if !current_user.nil? && current_user.favorite?(@divelog) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>

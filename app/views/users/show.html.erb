<% provide(:title, "プロフィール") %>
<div class="container">
  <h3 class="title-profile">プロフィール</h3>
  <div class="row">
    <div class="col-md-4">
      <div class="user-picture-box">
        <%= gravatar_for @user, size: 120, cl: "gravatar user-picture" %>
      </div>
    </div>
    <div class="row">
    <div class="col-md-9">
      <% if @user.divelogs.any? %>
        <h3>ダイブログ (<%= @user.divelogs.count %>)</h3>
        <ol class="divelogs">
          <%= render @divelogs %>
        </ol>
        <%= will_paginate @divelogs %>
      <% end %>
    </div>
  </div>
    <div class="col-md-8">
      <h4 class="show-user-name"><%= @user.name %></h4>　<%= @user.sex %>
      <div>
       経験本数　<%= @user.experience %>本　 指導団体　<%= @user.organization %> 　<%= @user.license %>
      </div>
      <div>
        <%= @user.introduction %>
      </div>
      <% if current_user == @user %>
        <div class="account-edit"><%= link_to "プロフィール編集", edit_user_path(@user) %></div>
      <% end %>
    </div>
  </div>
</div>
